# pickup changelog

## 0.0.1

- Initial release of `pickup`: `pickup watch` and `pickup stream` monitor a single PR, a repo, or an org (including GHES) for `@pickup` mentions and reply with explanations or generated fixes.
